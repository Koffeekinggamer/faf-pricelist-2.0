# Python
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (Husky / lint-staged / Prettier — commit hooks only)
node_modules/

# Local data — keep price book DB private (not on GitHub)
*.db
*.db-journal
*.db-wal
*.db-shm
master_pricebook.db

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Secrets / env
.env
.env.*
!.env.example
*.pem
credentials.json

# Streamlit
.streamlit/secrets.toml

# Login remember-me (signed token + HMAC key, local machine only)
.login_secret
.login_token

# Floor iPad pins (local preference)
.floor_favorites.json

# Temp / analysis
/tmp/
*.log
smoke_test_quote.pdf

# 2.0 data dir
data/*.db
data/
!.gitkeep

# Christina's lesson log (lives next to the DB; not source)
christina_lessons.jsonl
