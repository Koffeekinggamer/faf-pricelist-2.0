# Agent instructions — FAF Pricelist 2.0

1. Read **docs/HANDOFF.md** first.
2. This is a **greenfield** project. Do not continue hacking `~/FAF-pricebook` for 2.0 features.
3. Phase 1: Drop Excel → **on-screen PDF** + Admin backup/multipliers.
4. Never commit `*.db`, `.env`, or `.streamlit/secrets.toml`.
5. Prefer port **8510** locally so v1 can stay on **8501**.
