# Retail = wholesale × multiplier

Floor-facing price is always computed from builder **wholesale** × per-vendor **multiplier**, rounded to cents. Default multiplier is **2.7**; **Genuine Oak** is **1.7**. Saved vendor mults beat workbook markup unless the importer explicitly opts into markup. Search shows RETAIL; imports store `price_basis = wholesale`.

**Status:** accepted
