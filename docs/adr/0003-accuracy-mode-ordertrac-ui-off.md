# Accuracy mode; OrderTrac UI off

The shipped product is catalog accuracy: Search · Drop files · Vendors · Admin in `pricebook_app.py`. OrderTrac quote/connection UI stays behind `SHOW_ORDERTRAC_* = False`. Do not re-enable those flags unless Judson asks — backend hooks may remain for a later phase.

**Status:** accepted
