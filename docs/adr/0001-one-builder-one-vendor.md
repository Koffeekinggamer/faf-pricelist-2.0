# One builder = one vendor

Catalog identity is the builder’s canonical display name. Never store the same factory twice under filename stems, year suffixes, or HTML-entity twins (`D amp E` vs `D & E`). Re-import defaults to **replace_vendor** so that builder’s book is swapped whole — append is only for intentional duplicate rows.

**Status:** accepted
